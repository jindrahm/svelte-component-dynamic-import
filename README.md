# svelte-component-dynamic-import
Demonstrates bug in svelte - when svelte component is dynamically imported and another component is rendered within its slot, the child component's onMount is not called.
